import FormCard from './FormCard';

function Form() {
  return (
    <div className="relative flex items-center justify-center min-h-screen">
        <FormCard />
    </div>
  );
}

export default Form;
